runtime:
  layout: "thin"
  command: "sfs"
  version: "0.5.28-product"
state:
  dir: ".sfs-local"
overrides:
  sprint_templates: ".sfs-local/sprint-templates"
  decisions_template: ".sfs-local/decisions-template"
  personas: ".sfs-local/personas"
